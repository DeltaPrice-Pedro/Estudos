import 'package:flutter/material.dart';

class History extends StatelessWidget{
    const History({super.key})

    @override
    Widget build(context){
        return Column(children: [Text('Histórico')])
    }
}